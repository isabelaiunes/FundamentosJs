// Math é um objeto.
// Função "pow" (power) calcula potência.

const raio = 5.6;
const area = Math.PI + Math.pow(raio, 2);

console.log(area);
console.log(typeof Math);

// Math.floor()  → arredonda para baixo
// Math.ceil()   → arredonda para cima
// Math.round()  → arredonda para o inteiro mais próximo
// Math.trunc()  → remove as casas decimais (sem arredondar)
// toFixed()     → arredonda e retorna como texto (string)
